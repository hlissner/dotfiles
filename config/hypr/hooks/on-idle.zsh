#!/usr/bin/env zsh
# When Noctalia enters/exits the idle state.
#
# SYNOPSIS:
#   hey hook on-idle [--on|--off]
#
# DESCRIPTION:
#   Triggered by Noctalia's power settings (Power > Settings > Idle).

case $1
     --on)  ;;
     --off) ;;
esac
