# Lemonbar Config

+ `panel.sh`: runs my bar config
+ `panel_update.sh [LEMON]`: refreshes a lemon (and segment in the bar)
+ `panel_pacman_hook.sh`: refreshes the 'updates' segment, to be called from
  /etc/pacman.d/hooks/lemonbar.hook
