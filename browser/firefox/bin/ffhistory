#!/usr/bin/env python

from pyfzf import FzfPrompt
from lib.FirefoxProfile import FirefoxProfile


with FirefoxProfile.default() as ff:
    choice = FzfPrompt().prompt(ff.history())
    if choice:
        ff.browse("http" + choice[0].split(", http")[1])
    else:
        print("Aborted")
