#!/usr/bin/env bash
case $OSTYPE in
    darwin*)
        terminal-notifier -title "$1" -message "$2"
        ;;
    linux*)
        DISPLAY=:0 DBUS_SESSION_BUS_ADDRESS=unix:path=/run/user/1000/bus notify-send "$1" "$2" -t 0
        ;;
esac
