## Summary

- Import and wire the end4 `ii` Quickshell desktop path so Axiom defaults to `quickshell --config ii` instead of the legacy `axiom-shell` path.
- Add the adjacent end4 matugen, fuzzel, Hyprland, hypridle, and hyprlock sources while keeping Axiom host overrides generated by Nix.
- Document imported/omitted upstream paths, generated-state boundaries, security/privacy notes, and remaining live-session risks.

## Verification

- PASS: `config/quickshell/ii/shell.qml` exists and references `IllogicalImpulseFamily`.
- PASS: targeted `nix eval` confirmed `configName = "ii"`, Quickshell `ExecStart`, linked `quickshell/ii` and `matugen`, and generated Hyprland variables.
- PASS: static QML local-import scan checked 577 QML files with 0 missing local imports.
- PASS: generated-output and common-secret scans.
- PASS: wrapped Quickshell package build and full Axiom toplevel build.
- EXPECTED HEADLESS LIMITATION: offscreen Quickshell smoke reached `ii/shell.qml` before failing on missing PanelWindow backend.

## Risks/Follow-ups

- Live `systemctl --user restart quickshell.service` and hardware/UI exercise still need to run inside the actual Axiom Hyprland session.
- `cliphist` retention remains privacy-sensitive and needs retention/clear policy follow-up.
- Imported AI/cloud/function tooling should receive policy/documentation hardening before broader enablement.
- Some upstream optional commands remain KDE/kitty/fish/Arch-oriented and may need Axiom/Nix defaults or hiding.

## Legion Evidence

- Contract: `.legion/tasks/dots-hyprland-desktop-rfc/plan.md`
- RFC: `.legion/tasks/dots-hyprland-desktop-rfc/docs/rfc.md`
- Import manifest: `.legion/tasks/dots-hyprland-desktop-rfc/docs/import-manifest.md`
- Test report: `.legion/tasks/dots-hyprland-desktop-rfc/docs/test-report.md`
- Change review: `.legion/tasks/dots-hyprland-desktop-rfc/docs/review-change.md`
- Walkthrough: `.legion/tasks/dots-hyprland-desktop-rfc/docs/report-walkthrough.md`
