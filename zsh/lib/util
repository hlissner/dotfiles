
# Check if a symlink exists before trying to create it
function do_link {
    if [ -e "$2" ]; then
        rm -rf "$2"
    fi
    if [ ! -e "$2" ]; then
        ln -sf "$1" "$2"
    fi
}

# vim:set ft=zsh:
