[user]
	name = Henrik Lissner
	email = henrik@lissner.net
[github]
	user = hlissner

[core]
    excludesfile = ~/.gitignore
    quotepath = false
    whitespace = trailing-space

[color]
    ui = auto

[diff]
    tool = gvimdiff

[difftool]
    prompt = false

[merge]
    # Include summaries of merged commits in newly created merge commit messages
    log = true

[push]
    default = matching

[url "ssh://git@github.com"]
    insteadOf = "gh:"
[url "ssh://git@github.com/hlissner"]
    insteadOf = "gh:~"

[url "ssh://git@bitbucket.org"]
    insteadOf = "bb:"
[url "ssh://git@bitbucket.org/hlissner"]
    insteadOf = "bb:~"
